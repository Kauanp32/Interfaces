package devices;

// Definição da interface 'Scanner' no pacote 'devices'
public interface Scanner {

    // Declaração do método 'scan', que deve ser implementado pelas classes que implementam esta interface
    String scan();
}