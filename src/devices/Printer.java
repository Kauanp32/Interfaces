package devices;

// Definição da interface 'Printer' no pacote 'devices'
public interface Printer {

    // Declaração do método 'print', que deve ser implementado pelas classes que implementam esta interface
    void print(String doc);
}